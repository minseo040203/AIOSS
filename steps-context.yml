name: Secrets Example

on: push

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Use secret
        run: echo "API_KEY is ${{ secrets.API_KEY }}"
        env:
          API_KEY: ${{ secrets.API_KEY }}